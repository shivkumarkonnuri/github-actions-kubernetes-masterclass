name: CI

on:
  push:
    branches: [main]
    paths-ignore:
      - 'k8s/**'
      - 'docs/**'
      - '**/*.md'

permissions:
  contents: read
  security-events: write

jobs:

  # ── Static analysis / scan jobs (all run in parallel) ──────────────────────
  # actions/setup-go restores the module cache automatically on every run
  # (keyed on go.sum), so a separate warm-cache job is not needed.
  code-quality:
    uses: ./.github/workflows/code-quality.yml

  secrets-scan:
    uses: ./.github/workflows/secrets-scan.yml

  dependency-scan:
    uses: ./.github/workflows/dependency-scan.yml
    secrets: inherit

  docker-lint:
    uses: ./.github/workflows/docker-lint.yml

  # ── Build — backend and frontend run in parallel ────────────────────────────
  docker-build-push:
    needs: [code-quality, secrets-scan, dependency-scan, docker-lint]
    uses: ./.github/workflows/docker_build_push.yml
    secrets: inherit

  # ── Scan — backend and frontend run in parallel ─────────────────────────────
  docker-image-scan:
    needs: [docker-build-push]
    uses: ./.github/workflows/docker-image-scan.yml
    secrets: inherit
    with:
      image_tag: ${{ github.sha }}
